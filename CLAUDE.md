# CLAUDE.md — To Fairy 개발 가이드

> 이 파일은 Claude Code(및 모든 기여자)가 To Fairy를 개발할 때 따르는 최상위 지침이다.
> 제품 기획의 단일 출처(source of truth)는 `../To_Fairy_제품_메커니즘_설계.md`. 충돌 시 기획서가 우선한다.

---

## 0. 한 줄 정의
부모가 자녀를 감시하는 도구가 아니라, **자녀(만 7~9세)가 '핸드폰 요정'과 함께 자기 핸드폰 사용을 돌보는 관계형 동반자**.
스크리닝 신호는 자녀 폰 안(on-device)에 머물고, 부모에게는 '무엇을 봤는지'가 아니라 **요약된 맥락**만 전달된다.

---

## 1. 절대 불변식 (Invariants) — 이걸 깨는 코드는 머지 금지
이 7개는 제품의 정체성이자 법적 방어선이다. **편의·성능·기능을 이유로도 위반하지 않는다.**

1. **원시 로그는 폰을 떠나지 않는다.** 화면 콘텐츠 원문, 앱별 상세 사용 로그, 어떤 영상/페이지를 봤는지는 *서버로 전송하지 않는다.* 서버로 가는 것은 가명·집계된 '맥락 다이제스트'뿐이며, **그 다이제스트마저 E2EE로 암호화**되어 백엔드는 평문을 보지 못한다(`docs/50` §1). 자녀 폰에서 암호화, 부모 폰에서만 복호.
2. **콘텐츠 라벨은 즉시 판단 후 폐기한다.** A축 분류 결과(공포/성적/폭력 등 민감 라벨)는 그 순간 의사결정에만 쓰고 **영속 저장하지 않는다.** (개인정보 보호법 §23 민감정보 리스크 회피)
3. **요정은 생성된 문장을 말하지 않는다.** 아이가 듣는 모든 발화는 **사람이 작성·검수한 응답 뱅크에서 *선택*** 된다. LLM/분류기는 *의도(intent)만* 고른다. **온디바이스 LLM(Gemma 4 E2B 등)을 쓰더라도 그 출력은 function call/분류 결과로만 소비**하고, 자유 텍스트가 실시간으로 아이에게 도달하는 경로는 존재해선 안 된다. (creepy 제로 보장)
4. **부모의 사용은 절대 스크리닝하지 않는다.** (케이스 B) '요정 모드' 세션 경계가 곧 데이터 경계다. 세션 밖 = 수집 0.
5. **차단 기준은 국가 연령등급(연령가)이다.** 자체 도덕 판단으로 차단하지 않는다. 등급 있는 콘텐츠는 공식 등급, 없는 콘텐츠는 동일 루브릭으로 '등가 연령가' 추정. 임계는 보수적으로(놓치기보다 한 번 더 묻기).
6. **법정대리인(부모) 동의 없이는 자녀 데이터 처리를 시작하지 않는다.** 동의 확인(개인정보 보호법 시행령 §17조의2) 완료가 모든 센싱의 선행 게이트.
7. **아이에게 향하는 모든 문구는 쉬운 언어**(제22조의2③ 기준)로 작성한다. 비난·훈계·비밀 강요 금지.

> Claude Code에게: 위 불변식과 충돌하는 요구가 들어오면 **구현하지 말고 플래그**하라. "서버에서 콘텐츠 분석", "사용 기록 백업", "요정이 자유 대화", "부모가 상세 로그 열람" 류 요청이 대표적 위반 신호다.

---

## 2. 기술 스택 (v1, 잠정 — 확정 전 `@ahn` 승인 필요)
| 영역 | 선택 | 이유 |
|---|---|---|
| 자녀 앱 | **Android 네이티브 Kotlin + Jetpack Compose** | AccessibilityService·UsageStatsManager·DevicePolicyManager가 네이티브 강제. 크로스플랫폼 불가 |
| 부모 앱 | Android 네이티브 Kotlin/Compose (v1) | 일관성. 부모 앱은 스크리닝이 없어 iOS 확장은 후순위로 가능 |
| 온디바이스 ML | **Gemma 4 E2B** (후보) via MediaPipe LLM Inference / MLC | 멀티모달·function calling. 라우터 intent + A축 보조. **단 LLM은 화자 아님(#3)**. 저사양 폴백 분리 |
| 백엔드 | **FastAPI + PostgreSQL** (구현 **후순위**, 현재 **API 명세만** — `docs/40`) | 계정·동의확인·페어링(키교환)·E2EE 다이제스트 중계 |
| E2EE 암호화 | **libsodium (또는 Tink)** + Android Keystore | 다이제스트 종단간 암호화·키 보관 (`docs/50` §1) |
| 응답 뱅크 | 구조화 JSON 스키마 + 오디오 **앱 번들 내장** | §10 라우터+뱅크. **자산 스토리지·OTA 불필요**(앱 업데이트로 갱신) |

> iOS 자녀 앱은 **v1 명시적 제외**(ScreenTime API 한계로 행위 스크리닝 불가).

---

## 3. 모노레포 구조 (제안)
```
to-fairy/
├── apps/
│   ├── child-android/      # 자녀 앱: 센싱·스크리닝·요정  (앱 가이드: child-android/CLAUDE.md)
│   └── parent-android/     # 부모 앱: 맥락 다이제스트·페어링·동의 플로우
├── services/
│   └── backend/            # FastAPI (구현 후순위) — 계정·동의확인·페어링·E2EE 다이제스트 중계
├── packages/
│   ├── response-bank/      # intent 스키마·대사 원본·오디오 (앱 번들로 내장)
│   └── screening/          # 연령가 매핑·온디바이스 모델·신호 규칙 정의
└── docs/                   # 개발 문서
```
> **자산 스토리지/OTA 없음**: 응답뱅크·오디오·모델은 앱 번들에 내장하고 앱 업데이트로 갱신한다.

---

## 4. 핵심 도메인 개념 (용어 통일)
- **A축 (적절성)**: *지금 보는 것*의 연령가 판정 → 임계 초과 시 개입. 등급 있음=공식 등급, 없음=등가 추정.
- **B축 (중독성)**: *어떻게 쓰는가*의 패턴 신호(세션 길이·무한스크롤·전환 속도·취침 전·갈망). 콘텐츠 무관, 순수 사용패턴.
- **라우터(Router)**: 상황 → intent 분류기. 발화를 *짓지 않고* 고를 intent만 결정.
- **응답 뱅크(Response Bank)**: intent별 사람 작성·검수 대사 + 오디오. 아이가 듣는 모든 말의 출처.
- **맥락 다이제스트(Context Digest)**: 부모에게 가는 유일한 산출물. 패턴 + 개입 결과 + 대화 제안. 원시 로그 0.
- **케이스 A / B**: A=자녀 전용폰+부모폰 페어링 / B=자녀가 부모폰을 '요정 모드'로 공유 사용.

---

## 5. 컴플라이언스 훅 (코드가 법과 만나는 지점)
기능을 구현할 때 아래 지점은 반드시 대응 코드를 동반한다. (근거: `../To_Fairy_제품_메커니즘_설계.md` §8)
- **센싱 시작 전** → 법정대리인 동의 확인 상태 검증 (§17조의2). 미완료면 센싱 비활성.
- **A축 분류 직후** → 민감 라벨 **메모리에서만** 사용, 영속 저장소 기록 금지 (§23).
- **다이제스트 생성** → 민감 라벨 직접 노출 금지, 행동 제안으로 추상화.
- **케이스 B 세션** → 세션 밖 이벤트는 수집 파이프라인 진입 자체를 차단.
- **다이제스트 송신 전** → **E2EE 암호화 필수**. 평문이 네트워크/백엔드에 노출되는 경로 0 (`docs/50` §1).
- **아이 대상 고지 화면** → 쉬운 언어 카피 + 투명성 표시("요정이 ~라고 전했어").

---

## 6. 코딩 컨벤션 (초안)
- 주석·문서는 한국어, 코드 식별자는 영어.
- 자녀 데이터를 다루는 클래스/함수에는 `// INVARIANT:` 주석으로 어떤 불변식을 지키는지 명시.
- 원시 신호를 다루는 타입은 `Ephemeral*` 접두사로 명명해 '저장 금지' 의도를 타입 차원에서 드러낸다.
- 서버로 나가는 페이로드는 단일 `DigestPayload` 타입을 통과해야만 전송 가능(직접 네트워크 호출 금지) → 유출 경로 단일화·감사 용이.

---

## 7. 문서 맵
- `../To_Fairy_제품_메커니즘_설계.md` — 제품 기획 (source of truth)
- `docs/00_아키텍처_개요.md` — 시스템 아키텍처 (본 작업의 출발점)
- `apps/child-android/CLAUDE.md` — **자녀 앱 개발 가이드 (구체화 완료)**
- `docs/10_자녀앱_설계.md` — *예정* (상세 화면·상태흐름)
- `docs/11_부모앱_설계.md` — *예정*
- `docs/20_스크리닝_엔진.md` — *예정* (A축 연령가 매핑 + B축 신호 규칙)
- `docs/30_대화_응답뱅크.md` — *예정* (intent 스키마·라우터·오디오 파이프라인)
- `docs/40_백엔드_API.md` — **API 명세 완료** (구현 후순위)
- `docs/50_데이터_프라이버시_구현.md` — **시작됨** (§1 다이제스트 E2EE 완료, §2~4 예정)
- `docs/60_로드맵_마일스톤.md` — *예정*
